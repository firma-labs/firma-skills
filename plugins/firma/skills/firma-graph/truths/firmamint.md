---
name: firmamint
description: Firma's own chain. Compute and liquidity. One chain; every -mint protocol is a layer on it. On public surfaces it is "the compute substrate".
type: truth
status: LOCKED
source: firma-vault wiki/protocol-master-knowledge.md; FIRMA-PORTABLE-PRIMER.md
---

# firmamint

firmamint is the chain. Its identity beside the others: Bitcoin stores value, Ethereum settles, Solana is fast, Base is agentic, firmamint is compute and liquidity. Not two chains; "Firmamint Compute Chain" was a wrong framing. Compute is what firmamint is known for. Edge sells compute to agents, Capital funds the energy and data centers, economic rights are earned by contributing compute.

**Every -mint protocol is a layer on firmamint**, the way Uniswap is a layer on Ethereum: FIG, [[elemint]], [[atonemint]], Settlemint, [[embodimint]], Governmint, Testamint, MintwareOS, 1Accord, [[basemint]]. Group them under firmamint in any document; never list them as peers of the chain.

**Stack order:** firmamint → Firmagentics (species genome) → [[dna-protocol]] (organism genome) → [[theos]] → theo and the Ambassador. Gas: OP Stack, ETH, an ERC-4337 lane. The Paymaster is the on-chain gas contract. Direct ETH gas is the escape hatch so nothing can censor.

**Public copy** leads with "the compute substrate" and defines it in one plain line the first time it appears. Keep the word "chain" only where it is technically the chain. Anchor lines: "The compute runs **on** firmamint." "The power runs **through** firmamint." Compute executes on; power flows through. See [[earning-by-giving]].

Always lowercase. See [[names-and-casing]]. Today's Base surfaces are the intro version; see [[colony-intro-version]].
